pipeline {
    agent any

    stages {
        stage('docker-build') {
            steps {
                // sh 'docker build -t donkeysharp/juice-shop:latest .'
                sh 'echo hello world!'
                sh 'ls -l'
            }
        }
    }
}
